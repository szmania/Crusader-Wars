
attacker_army_data = {
    effect = {
        debug_log_scopes = yes
        set_variable = {name = attacker_army_var value = scope:attacker_army_data}
    }
}

siege_province_data = {
    effect = {
        debug_log_scopes = yes
        set_variable = {name = siege_province_var value = scope:siege_province_data}
    }
}
siege_data = {
    effect = {
        debug_log_scopes = yes
        set_variable = {name = siege_id_var value = scope:siege_data.GetID}
        set_variable = {name = siege_province_id_var value = scope:siege_data.GetProvince.GetID}
    }
}
holding_level = {
    effect = {
        debug_log_scopes = yes
        set_variable = {name = holding_level_var value = scope:holding_level}
    }
}
special_building = {
    effect = {
        debug_log_scopes = yes
        set_variable = {name = unique_map_key_var value = scope:special_building.GetSpecialBuildingType.GetKey}
    }
}
