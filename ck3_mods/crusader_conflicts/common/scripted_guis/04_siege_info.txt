
attacker_side = {
    effect = {
        debug_log_scopes = yes
        set_variable = {name = attacker_army value = scope:attacker_side}
    }
}
siege_data = {
    effect = {
        debug_log_scopes = yes
        set_variable = {name = siege_var value = scope:siege_data}
    }
}
holding_level = {
    effect = {
        debug_log_scopes = yes
        set_variable = {name = holding_level_var value = scope:holding_level}
    }
}
